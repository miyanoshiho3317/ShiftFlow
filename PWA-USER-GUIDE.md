# ShiftFlow PWA 使用说明

ShiftFlow 可以作为网页应用（PWA）安装到电脑或手机。安装后会以独立窗口运行，首次成功打开后可在没有网络时继续查看已缓存的排班日历和导出 `.ics` 文件。

## 使用前提

- 使用已部署的 HTTPS 网站，例如 GitHub Pages 地址。
- 本地调试时使用 `http://localhost` 启动静态服务器。
- 不要直接双击打开 `index.html`：`file://` 页面不能启用 Service Worker，因此不能提供离线缓存或完整安装体验。

首次安装前，请在有网络的环境下打开一次页面并等待加载完成。浏览器会下载页面、样式、脚本和应用图标，以供之后离线使用。

## 在电脑上安装

### Chrome 或 Edge

1. 打开 ShiftFlow 的 HTTPS 地址。
2. 等待页面加载完成；地址栏右侧出现安装图标时点击它。
3. 选择“安装 ShiftFlow”或“安装应用”。

安装后可从浏览器的应用列表或系统启动器打开。它会显示为独立窗口，而不是普通浏览器标签页。

如果地址栏没有安装图标，可打开浏览器菜单，选择“安装 ShiftFlow”或“安装此网站作为应用”。

## 在 Android 手机上安装

1. 用 Chrome 打开 ShiftFlow 的 HTTPS 地址。
2. 点击右上角菜单。
3. 选择“安装应用”或“添加到主屏幕”。
4. 确认名称和图标后完成安装。

## 在 iPhone 或 iPad 上安装

1. 用 Safari 打开 ShiftFlow 的 HTTPS 地址。
2. 点击底部或顶部的“分享”按钮。
3. 选择“添加到主屏幕”。
4. 确认后点击“添加”。

iOS 不一定显示 Chrome/Edge 那样的“安装”提示，但添加到主屏幕后同样可以像应用一样启动。

## 离线使用

首次在线打开成功后，可以用以下方式确认离线可用：

1. 关闭页面并断开网络。
2. 从主屏幕、桌面或浏览器应用列表重新打开 ShiftFlow。
3. 页面应能正常打开并保留当前设备已经保存的排班锚点、配色和单日调班记录。

离线时无法获取尚未缓存的新版本；重新联网并打开页面后，浏览器会下载更新。

## 更新说明

ShiftFlow 会在发布新版本后自动清理旧版缓存。若页面仍显示旧内容：

1. 保持联网并关闭所有 ShiftFlow 窗口。
2. 重新打开应用或网页，并进行一次刷新。
3. 如仍无效，在浏览器的网站设置中清除该站点的数据后重新访问。

清除站点数据会删除本设备保存的排班锚点、配色和单日调班记录；如需保留排班结果，请先导出 `.ics` 文件。

## 常见问题

### 为什么不能安装？

确认网页使用 HTTPS（或本机 `localhost`），并且网络没有拦截页面资源。直接打开本地 HTML 文件不满足 PWA 的安全要求。

### 为什么离线时第一次打不开？

必须先在线完整打开一次，让浏览器完成离线缓存。首次访问时断网无法安装或缓存页面。

### 为什么更新后内容没变化？

PWA 会优先使用本地缓存。联网后关闭并重新打开页面，或刷新一次，即可让浏览器检查更新。

## 隐私

排班计算、配色与单日调班记录仅保存在当前浏览器设备中。PWA 缓存只保存应用运行所需的静态文件，不会上传排班数据。
